import { ProActiveChatComponentRoutingModule } from './pro-active-chat-component-routing.module';

describe('ProActiveChatComponentRoutingModule', () => {
  let proActiveChatComponentRoutingModule: ProActiveChatComponentRoutingModule;

  beforeEach(() => {
    proActiveChatComponentRoutingModule = new ProActiveChatComponentRoutingModule();
  });

  it('should create an instance', () => {
    expect(proActiveChatComponentRoutingModule).toBeTruthy();
  });
});
